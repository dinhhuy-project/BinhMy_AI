# Frontend Environment Variables
VITE_API_URL=/api
VITE_GEMINI_API_KEY=your_google_gemini_api_key_here

# Backend Environment Variables
PORT=5000
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/?appName=AISearch
DATABASE_NAME=ai_image_finder
NODE_ENV=production

# For Railway Deployment
# VITE_GEMINI_API_KEY should be set to your Gemini API key from https://aistudio.google.com
# MONGODB_URI should be set to your MongoDB Atlas connection string
# The app will serve both frontend and API from the same Railway instance
